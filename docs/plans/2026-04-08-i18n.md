# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add next-intl URL-based internationalization (ru/fr/de/es) to the web app — UI text only, exercises unchanged.

**Architecture:** All web routes move under `app/[lang]/`. `proxy.ts` (Next.js 16) wraps next-intl's `createMiddleware` for locale detection and redirect. `NextIntlClientProvider` in the root locale layout makes translations available to all Client Components via `useTranslations`. Server Components use `getTranslations`.

**Tech Stack:** next-intl, Next.js 16 (`proxy.ts` instead of `middleware.ts`), `LayoutProps<'/[lang]'>` / `PageProps<'/[lang]'>` TypeScript helpers.

**Design spec:** `docs/architecture/2026-04-07-i18n-design.md`

---

## File Map

**New files:**

- `shared/i18n/config.ts` — locale list, routing config
- `shared/i18n/request.ts` — next-intl `getRequestConfig` (server message loading)
- `shared/i18n/navigation.ts` — locale-aware `Link`, `useRouter`, `usePathname`, `redirect`
- `shared/i18n/messages/ru.json` — Russian translations (complete)
- `shared/i18n/messages/fr.json` — French translations
- `shared/i18n/messages/de.json` — German translations
- `shared/i18n/messages/es.json` — Spanish translations
- `proxy.ts` — locale routing (wraps next-intl middleware)
- `app/[lang]/layout.tsx` — root locale layout: `<html lang={lang}>`, `<body>`, fonts, `SerwistProvider`, `NextIntlClientProvider`
- `app/telegram/layout.tsx` — minimal html+body for telegram (no i18n yet)
- `app/[lang]/(web)/layout.tsx` — web shell: Header, Footer, InstallBanner, SyncProvider
- `app/[lang]/(web)/page.tsx` — home page
- `app/[lang]/(web)/tense-trainer/page.tsx`
- `app/[lang]/(web)/profile/page.tsx`
- `app/[lang]/(web)/settings/page.tsx` (fixes the `settings.tsx/` bug from old structure)
- `app/[lang]/(web)/error.tsx`

**Modified files:**

- `next.config.ts` — add `withNextIntl` plugin
- `presentation/web/components/Header/Header.tsx` — locale-aware Link/usePathname + language selector
- `presentation/web/pages/Home/Home.tsx` — `getTranslations`, remove hardcoded strings
- `presentation/web/pages/Home/ui/Reason.tsx` — change prop from `getDescription: () => ReactNode` to `description: ReactNode`
- `presentation/web/pages/TenseTrainer/logic/tenseLabels.ts` — rename `label` → `labelKey` in `ITenseGroup`
- `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/SelectTrainingSection.tsx` — `useTranslations`
- `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/ModeSelector.tsx` — `useTranslations`, remove `MODE_LABELS` usage
- `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/TenseGroup.tsx` — add `'use client'`, `useTranslations`
- `presentation/web/pages/TenseTrainer/ui/TrainingSection/TrainingSection.tsx` — `useTranslations`
- `presentation/web/pages/TenseTrainer/ui/TrainingSection/TaskResult.tsx` — `useTranslations`
- `presentation/web/pages/Profile/Profile.tsx` — `useTranslations`
- `presentation/web/pages/Profile/ui/StatsOverview/StatsOverview.tsx` — `useTranslations`
- `presentation/web/pages/Profile/ui/SessionHistory/SessionHistory.tsx` — `useTranslations`, `useLocale` for date
- `presentation/web/pages/Profile/ui/SessionHistory/SessionDetail.tsx` — `useTranslations`
- `presentation/web/components/NetworkBadge/NetworkBadge.tsx` — `useTranslations`
- `presentation/web/components/InstallBanner/InstallBanner.tsx` — `useTranslations`
- `shared/config/training.ts` — remove `MODE_LABELS`

**Deleted files:**

- `app/layout.tsx` — replaced by `app/[lang]/layout.tsx`
- `app/(web)/layout.tsx`
- `app/(web)/page.tsx`
- `app/(web)/tense-trainer/page.tsx`
- `app/(web)/profile/page.tsx`
- `app/(web)/settings.tsx` (note: this was a misnamed file — `settings.tsx` instead of `settings/page.tsx`)
- `app/(web)/error.tsx`
- `presentation/web/pages/Home/logic/reasons.tsx` — content moves into translations

**Unchanged:**

- `app/manifest.json` — already has `start_url: "/"`, proxy redirects to correct locale on PWA launch
- `app/sitemap.ts`, `app/not-found.tsx`, `app/globals.css`, `app/telegram/` pages

> **Note on existing tests:** After adding `useTranslations` to components, tests that render those components will need a `NextIntlClientProvider` wrapper with mock messages. Update affected `__tests__/` files as you encounter failures.

---

## Task 1: Install next-intl and configure next.config.ts

**Files:**

- Modify: `next.config.ts`

- [ ] **Step 1: Install next-intl**

```bash
npm install next-intl
```

- [ ] **Step 2: Update next.config.ts to add the plugin**

```ts
import { withSerwist } from '@serwist/turbopack';
import createNextIntlPlugin from 'next-intl/plugin';
import type { NextConfig } from 'next';

const withNextIntl = createNextIntlPlugin('./shared/i18n/request.ts');

const nextConfig: NextConfig = {};

export default withNextIntl(withSerwist(nextConfig));
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors (next-intl types are registered by the plugin).

- [ ] **Step 4: Commit**

```bash
git add next.config.ts package.json package-lock.json
git commit -m "feat: install next-intl"
```

---

## Task 2: Create i18n foundation (config, request, navigation)

**Files:**

- Create: `shared/i18n/config.ts`
- Create: `shared/i18n/request.ts`
- Create: `shared/i18n/navigation.ts`

- [ ] **Step 1: Create `shared/i18n/config.ts`**

```ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
	locales: ['ru', 'fr', 'de', 'es'],
	defaultLocale: 'ru',
});

export type Locale = (typeof routing.locales)[number];
```

- [ ] **Step 2: Create `shared/i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './config';

export default getRequestConfig(async ({ requestLocale }) => {
	const requested = await requestLocale;
	const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale;

	return {
		locale,
		messages: (await import(`./messages/${locale}.json`)).default,
	};
});
```

- [ ] **Step 3: Create `shared/i18n/navigation.ts`**

```ts
import { createNavigation } from 'next-intl/navigation';
import { routing } from './config';

export const { Link, useRouter, usePathname, redirect } = createNavigation(routing);
```

- [ ] **Step 4: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add shared/i18n/
git commit -m "feat: add next-intl config, request, and navigation"
```

---

## Task 3: Create Russian translation messages

**Files:**

- Create: `shared/i18n/messages/ru.json`

- [ ] **Step 1: Create `shared/i18n/messages/ru.json`**

```json
{
	"common": {
		"loading": "Загрузка..."
	},
	"nav": {
		"home": "Главная",
		"trainer": "Тренажер",
		"mainNavLabel": "Основная навигация",
		"profileAriaLabel": "Профиль пользователя",
		"languageSelectorLabel": "Выбор языка"
	},
	"home": {
		"tagline": "Практикуй английские времена офлайн",
		"heroDescription": "Путаница с <bold>английскими временами</bold> — одна из самых частых проблем в изучении <bold>языка</bold>. Не потому что правила сложные, а потому что их нужно чувствовать в контексте. Этот <bold>тренажёр</bold> именно для этого.",
		"startTraining": "Начать тренировку",
		"startTrainingAriaLabel": "Начать тренировку по английским временам",
		"whyHeading": "Почему этот тренажёр",
		"privacy": {
			"title": "Ваши данные остаются у вас",
			"localStorage": {
				"title": "Локальное хранилище",
				"description": "Весь прогресс хранится в вашем браузере — никуда не уходит."
			},
			"noTracking": {
				"title": "Без слежки",
				"description": "Мы не собираем, не храним и не передаём никакие данные о вас."
			},
			"offline": {
				"title": "Работает офлайн",
				"description": "После первой загрузки приложение работает без интернета."
			}
		},
		"reasons": {
			"context": {
				"title": "Контекст решает всё",
				"description": "В английском одно и то же действие может передаваться разными <bold>временами</bold> в зависимости от смысла, который вы хотите выразить."
			},
			"forms": {
				"title": "12 времён — не миф",
				"description": "Каждое <bold>время</bold> имеет свои маркеры и логику. Без практики они смешиваются и перестают ощущаться интуитивно."
			},
			"noTools": {
				"title": "Рабочих тренажёров нет",
				"description": "Я не нашёл ни одного инструмента, который действительно <bold>тренирует</bold> выбор времени по контексту — поэтому создал его сам."
			}
		}
	},
	"trainer": {
		"title": "Tense Trainer",
		"subtitle": "Выбери времена для тренировки",
		"continue": "Продолжить",
		"selectAll": "Выбрать все",
		"clearAll": "Сбросить",
		"start": "Начать",
		"restart": "Начать заново",
		"back": "Назад",
		"finish": "Завершить",
		"inputPlaceholder": "Введи перевод на английском...",
		"skip": "Пропустить",
		"check": "Проверить",
		"correct": "Верно!",
		"incorrect": "Неверно",
		"correctAnswer": "Правильный ответ",
		"whyThisTense": "Почему это время",
		"next": "Следующее",
		"mode": "Режим",
		"modeFixed": "Фиксированное количество",
		"modeInfinite": "Бесконечный режим",
		"groupSelectAll": "Выбрать все",
		"groupDeselect": "Снять",
		"group": {
			"present": "Present",
			"past": "Past",
			"future": "Future"
		}
	},
	"profile": {
		"overallStats": "Общая статистика",
		"progress": "Прогресс",
		"byTenses": "По временам",
		"sessionHistory": "История сессий",
		"total": "Всего",
		"correct": "Правильно",
		"skipped": "Пропущено",
		"incorrect": "Неверно",
		"emptyHistory": "История пуста",
		"noData": "Нет данных",
		"noAnswers": "Нет ответов",
		"showMore": "Показать ещё {count}",
		"sessionCorrect": "{correct}/{attempted} верно",
		"skippedAnswer": "Пропущено",
		"correctAnswer": "Верно",
		"incorrectAnswer": "Неверно",
		"answer": "Ответ: {answer}",
		"allTenses": "Все времена",
		"presentTenses": "Present tenses",
		"pastTenses": "Past tenses",
		"futureTenses": "Future tenses"
	},
	"network": {
		"offline": "Offline",
		"slow": "Slow connection"
	},
	"install": {
		"title": "Install Tense Master",
		"description": "Use offline, faster access",
		"installButton": "Install"
	}
}
```

- [ ] **Step 2: Commit**

```bash
git add shared/i18n/messages/ru.json
git commit -m "feat: add Russian translation messages"
```

---

## Task 4: Create proxy.ts

**Files:**

- Create: `proxy.ts`

- [ ] **Step 1: Create `proxy.ts` at project root**

```ts
import createMiddleware from 'next-intl/middleware';
import type { NextRequest } from 'next/server';
import { routing } from './shared/i18n/config';

const handleI18nRouting = createMiddleware(routing);

export function proxy(request: NextRequest) {
	return handleI18nRouting(request);
}

export const config = {
	matcher: ['/((?!api|_next|telegram|.*\\..*).*)'],
};
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add proxy.ts
git commit -m "feat: add proxy.ts for next-intl locale routing"
```

---

## Task 5: Restructure app/ — create [lang] layout, move web pages

**Files:**

- Delete: `app/layout.tsx`
- Create: `app/[lang]/layout.tsx`
- Create: `app/telegram/layout.tsx`
- Delete: `app/(web)/layout.tsx`
- Create: `app/[lang]/(web)/layout.tsx`
- Delete: `app/(web)/page.tsx`
- Create: `app/[lang]/(web)/page.tsx`
- Delete: `app/(web)/tense-trainer/page.tsx`
- Create: `app/[lang]/(web)/tense-trainer/page.tsx`
- Delete: `app/(web)/profile/page.tsx`
- Create: `app/[lang]/(web)/profile/page.tsx`
- Delete: `app/(web)/settings.tsx`
- Create: `app/[lang]/(web)/settings/page.tsx`
- Delete: `app/(web)/error.tsx`
- Create: `app/[lang]/(web)/error.tsx`

- [ ] **Step 1: Create `app/[lang]/layout.tsx`**

This is the new root layout. It replaces `app/layout.tsx` entirely and adds locale support.

```tsx
import { cn } from '@/shared/lib/utils';
import { routing } from '@/shared/i18n/config';
import { SerwistProvider } from '@/shared/pwa/serwist';
import { hasLocale } from 'next-intl';
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { Geist, Geist_Mono, Inter } from 'next/font/google';
import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import '@/app/globals.css';

const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });
const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] });
const geistMono = Geist_Mono({ variable: '--font-geist-mono', subsets: ['latin'] });

export const metadata: Metadata = {
	metadataBase: new URL('https://tense-master.xyz'),
	title: 'Tense Master',
	description:
		'Практикуй английские времена — Present Simple, Past Perfect и другие — с упражнениями на перевод предложений',
	appleWebApp: { title: 'Tense Master', capable: true, statusBarStyle: 'default' },
	formatDetection: { telephone: false },
	openGraph: {
		title: 'Tense Master',
		type: 'website',
		siteName: 'Tense Master',
		description:
			'Практикуй английские времена — Present Simple, Past Perfect и другие — с упражнениями на перевод предложений',
		images: [{ url: '/og.png', width: 1200, height: 630 }],
	},
	twitter: {
		card: 'summary_large_image',
		title: 'Tense Master',
		description:
			'Практикуй английские времена — Present Simple, Past Perfect и другие — с упражнениями на перевод предложений',
		images: ['/og.png'],
	},
	icons: {
		icon: [
			{ url: '/favicon.ico' },
			{ url: '/favicon-96x96.png', sizes: '96x96', type: 'image/png' },
			{ url: '/favicon.svg', type: 'image/svg+xml' },
		],
		apple: '/apple-touch-icon.png',
	},
};

export function generateStaticParams() {
	return routing.locales.map(lang => ({ lang }));
}

export default async function LocaleLayout({
	children,
	params,
}: {
	children: React.ReactNode;
	params: Promise<{ lang: string }>;
}) {
	const { lang } = await params;
	if (!hasLocale(routing.locales, lang)) notFound();

	const messages = await getMessages();

	return (
		<html
			lang={lang}
			className={cn(
				'h-full',
				'antialiased',
				geistSans.variable,
				geistMono.variable,
				'font-sans',
				inter.variable,
			)}
		>
			<body className='flex min-h-full flex-col'>
				<NextIntlClientProvider messages={messages}>
					<SerwistProvider swUrl='/api/serwist/sw.js'>{children}</SerwistProvider>
				</NextIntlClientProvider>
			</body>
		</html>
	);
}
```

- [ ] **Step 2: Create `app/telegram/layout.tsx`** (minimal html+body for telegram routes, no i18n yet)

```tsx
export default function TelegramLayout({ children }: { children: React.ReactNode }) {
	return (
		<html lang='en'>
			<body>{children}</body>
		</html>
	);
}
```

- [ ] **Step 3: Create `app/[lang]/(web)/layout.tsx`**

```tsx
import Footer from '@/presentation/web/components/Footer';
import Header from '@/presentation/web/components/Header/Header';
import InstallBanner from '@/presentation/web/components/InstallBanner/InstallBanner';
import { SyncProvider } from '@/presentation/web/components/SyncProvider/SyncProvider';

export default function WebLayout({ children }: { children: React.ReactNode }) {
	return (
		<div className='flex min-h-screen flex-col'>
			<SyncProvider />
			<Header />
			<div className='flex flex-1 flex-col'>{children}</div>
			<InstallBanner />
			<Footer />
		</div>
	);
}
```

- [ ] **Step 4: Create `app/[lang]/(web)/page.tsx`**

```tsx
import HomePage from '@/presentation/web/pages/Home/Home';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Tense Master',
	description:
		'Практикуй английские времена — Present Simple, Past Perfect и другие — с упражнениями на перевод предложений',
};

const jsonLd = {
	'@context': 'https://schema.org',
	'@type': 'WebApplication',
	name: 'Tense Master',
	url: 'https://tense-master.xyz',
	description: 'Практикуй английские времена с переводом предложений',
	applicationCategory: 'EducationalApplication',
	inLanguage: 'ru',
	offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};

export default function Page() {
	return (
		<>
			<script
				type='application/ld+json'
				dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
			/>
			<HomePage />
		</>
	);
}
```

- [ ] **Step 5: Create `app/[lang]/(web)/tense-trainer/page.tsx`**

```tsx
import TenseTrainer from '@/presentation/web/pages/TenseTrainer/TenseTrainer';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Тренажер времен | Tense Master',
	description:
		'Тренажер английских времен Tense Master. Практикуйся в использовании всех 16 времен английского языка.',
	robots: { index: false, follow: false },
};

export default function TenseTrainerPage() {
	return <TenseTrainer />;
}
```

- [ ] **Step 6: Create `app/[lang]/(web)/profile/page.tsx`**

```tsx
import ProfilePage from '@/presentation/web/pages/Profile/Profile';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Профиль | Tense Master',
	description: 'Твой профиль в Tense Master. Статистика и история сессий.',
	robots: { index: false, follow: false },
};

export default function Page() {
	return <ProfilePage />;
}
```

- [ ] **Step 7: Create `app/[lang]/(web)/settings/page.tsx`**

```tsx
import SettingsPage from '@/presentation/web/pages/Settings/Settings';

export default function Page() {
	return <SettingsPage />;
}
```

- [ ] **Step 8: Create `app/[lang]/(web)/error.tsx`** (copy from old location)

```tsx
'use client';

import { Button } from '@/presentation/components/ui/button';

export default function Error({
	error,
	reset,
}: {
	error: Error & { digest?: string };
	reset: () => void;
}) {
	return (
		<div className='flex flex-1 flex-col items-center justify-center gap-4 p-6 text-center'>
			<p className='text-muted-foreground text-sm'>{error.message || 'Something went wrong'}</p>
			<div className='flex gap-2'>
				<Button variant='outline' onClick={() => window.location.reload()}>
					Reload page
				</Button>
				<Button onClick={reset}>Try again</Button>
			</div>
		</div>
	);
}
```

- [ ] **Step 9: Delete the old app/ files**

```bash
rm app/layout.tsx
rm app/\(web\)/layout.tsx
rm app/\(web\)/page.tsx
rm app/\(web\)/tense-trainer/page.tsx
rm app/\(web\)/profile/page.tsx
rm "app/(web)/settings.tsx"
rm app/\(web\)/error.tsx
```

- [ ] **Step 10: Verify the dev server starts and routes work**

```bash
npm run dev
```

Open `http://localhost:3000` — expected: redirect to `http://localhost:3000/ru/`.
Open `http://localhost:3000/fr/` — expected: renders the app in `/fr/` without errors.

- [ ] **Step 11: Commit**

```bash
git add app/
git commit -m "feat: restructure app/ routing under [lang] segment"
```

---

## Task 6: Update navigation imports to locale-aware versions

Header and Home currently import `Link` from `next/link` and `usePathname` from `next/navigation`. These must be replaced with locale-aware versions from `@/shared/i18n/navigation` so that links automatically include the active locale prefix.

**Files:**

- Modify: `presentation/web/components/Header/Header.tsx`
- Modify: `presentation/web/pages/Home/Home.tsx`

- [ ] **Step 1: Update `Header.tsx` — swap navigation imports**

Replace the import lines at the top:

```ts
// Remove:
import Link from 'next/link';
import { usePathname } from 'next/navigation';

// Add:
import { Link, usePathname } from '@/shared/i18n/navigation';
```

No other changes to Header yet — translations and language selector come in Task 10.

- [ ] **Step 2: Update `Home.tsx` — swap Link import**

```ts
// Remove:
import Link from 'next/link';

// Add:
import { Link } from '@/shared/i18n/navigation';
```

- [ ] **Step 3: Verify TypeScript and lint**

```bash
npm run typecheck && npm run lint:check
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add presentation/web/components/Header/Header.tsx presentation/web/pages/Home/Home.tsx
git commit -m "feat: use locale-aware Link and usePathname in Header and Home"
```

---

## Task 7: Translate Home page

**Files:**

- Modify: `presentation/web/pages/Home/Home.tsx`
- Modify: `presentation/web/pages/Home/ui/Reason.tsx`
- Delete: `presentation/web/pages/Home/logic/reasons.tsx`

The `reasons.tsx` static data file is deleted. Its content moves into `ru.json` (already done in Task 3). The three reasons are inlined in `Home.tsx` using `t.rich` for bold markup.

`Reason.tsx` is simplified: the `getDescription: () => ReactNode` prop becomes `description: ReactNode`.

- [ ] **Step 1: Update `Reason.tsx`**

```tsx
import type { ReactNode } from 'react';

interface IProps {
	title: string;
	description: ReactNode;
}

const Reason = ({ title, description }: IProps) => {
	return (
		<article className='border-border bg-card rounded-xl border p-6'>
			<h3 className='text-primary mb-2 font-semibold'>{title}</h3>
			<p className='text-muted-foreground text-sm leading-relaxed'>{description}</p>
		</article>
	);
};

export default Reason;
```

- [ ] **Step 2: Rewrite `Home.tsx` with translations**

`Home.tsx` is a Server Component (no `'use client'`), so it uses `getTranslations`.

```tsx
import { getTranslations } from 'next-intl/server';
import { Link } from '@/shared/i18n/navigation';
import { Button } from '@/presentation/components/ui/button';
import { ROUTES } from '@/shared/config/routes';
import { HardDriveIcon, ShieldOffIcon, WifiOffIcon } from 'lucide-react';
import Reason from './ui/Reason';

const HomePage = async () => {
	const t = await getTranslations('home');

	const reasons = [
		{
			key: 'context',
			title: t('reasons.context.title'),
			description: t.rich('reasons.context.description', { bold: chunks => <b>{chunks}</b> }),
		},
		{
			key: 'forms',
			title: t('reasons.forms.title'),
			description: t.rich('reasons.forms.description', { bold: chunks => <b>{chunks}</b> }),
		},
		{
			key: 'noTools',
			title: t('reasons.noTools.title'),
			description: t.rich('reasons.noTools.description', { bold: chunks => <b>{chunks}</b> }),
		},
	];

	return (
		<main className='bg-background text-foreground flex flex-1 flex-col'>
			<div className='mx-auto flex max-w-3xl flex-1 flex-col items-center justify-center gap-16 px-6 py-24'>
				<section
					aria-labelledby='home-heading'
					className='flex flex-col items-center gap-6 text-center'
				>
					<h1
						id='home-heading'
						className='border-border bg-card text-primary rounded-full border px-4 py-1.5 text-sm'
					>
						{t('tagline')}
					</h1>
					<span aria-hidden='true' className='text-foreground text-5xl font-bold tracking-tight'>
						Tense Master
					</span>
					<p className='text-muted-foreground max-w-xl text-lg leading-relaxed'>
						{t.rich('heroDescription', { bold: chunks => <b>{chunks}</b> })}
					</p>
					<Button asChild size='lg' className='mt-2'>
						<Link href={ROUTES.trainer} aria-label={t('startTrainingAriaLabel')}>
							{t('startTraining')}
						</Link>
					</Button>
				</section>

				<section aria-labelledby='reasons-heading' className='grid w-full gap-4 sm:grid-cols-3'>
					<h2 id='reasons-heading' className='sr-only'>
						{t('whyHeading')}
					</h2>
					{reasons.map(reason => (
						<Reason key={reason.key} title={reason.title} description={reason.description} />
					))}
				</section>

				<section
					aria-labelledby='privacy-heading'
					className='border-border bg-card w-full rounded-2xl border p-8'
				>
					<h2
						id='privacy-heading'
						className='text-foreground mb-6 text-center text-lg font-semibold'
					>
						{t('privacy.title')}
					</h2>
					<ul className='grid gap-4 sm:grid-cols-3'>
						<li className='flex items-start gap-3'>
							<HardDriveIcon className='text-primary mt-0.5 size-5 shrink-0' />
							<div>
								<p className='text-foreground text-sm font-medium'>
									{t('privacy.localStorage.title')}
								</p>
								<p className='text-muted-foreground text-sm'>
									{t('privacy.localStorage.description')}
								</p>
							</div>
						</li>
						<li className='flex items-start gap-3'>
							<ShieldOffIcon className='text-primary mt-0.5 size-5 shrink-0' />
							<div>
								<p className='text-foreground text-sm font-medium'>
									{t('privacy.noTracking.title')}
								</p>
								<p className='text-muted-foreground text-sm'>
									{t('privacy.noTracking.description')}
								</p>
							</div>
						</li>
						<li className='flex items-start gap-3'>
							<WifiOffIcon className='text-primary mt-0.5 size-5 shrink-0' />
							<div>
								<p className='text-foreground text-sm font-medium'>{t('privacy.offline.title')}</p>
								<p className='text-muted-foreground text-sm'>{t('privacy.offline.description')}</p>
							</div>
						</li>
					</ul>
				</section>
			</div>
		</main>
	);
};

export default HomePage;
```

- [ ] **Step 3: Delete `reasons.tsx`**

```bash
rm "presentation/web/pages/Home/logic/reasons.tsx"
```

- [ ] **Step 4: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors. (If any test imports `reasons.tsx`, update the test to remove that import.)

- [ ] **Step 5: Commit**

```bash
git add presentation/web/pages/Home/
git commit -m "feat: translate Home page"
```

---

## Task 8: Translate Trainer components

**Files:**

- Modify: `presentation/web/pages/TenseTrainer/logic/tenseLabels.ts`
- Modify: `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/SelectTrainingSection.tsx`
- Modify: `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/ModeSelector.tsx`
- Modify: `presentation/web/pages/TenseTrainer/ui/SelectTrainingSection/TenseGroup.tsx`
- Modify: `presentation/web/pages/TenseTrainer/ui/TrainingSection/TrainingSection.tsx`
- Modify: `presentation/web/pages/TenseTrainer/ui/TrainingSection/TaskResult.tsx`
- Modify: `shared/config/training.ts`

- [ ] **Step 1: Update `tenseLabels.ts` — rename `label` to `labelKey`**

The group label is no longer a display string but a translation key (`'present' | 'past' | 'future'`).

```ts
import { Tense, TenseType } from '@/domain/value-objects';
import { TENSES_GROUPS } from '@/shared/config/tenses';

export const TENSE_LABELS: Record<TenseType, string> = {
	[Tense.PRESENT_SIMPLE]: 'Present Simple',
	[Tense.PRESENT_CONTINUOUS]: 'Present Continuous',
	[Tense.PRESENT_PERFECT]: 'Present Perfect',
	[Tense.PRESENT_PERFECT_CONTINUOUS]: 'Present Perfect Continuous',
	[Tense.PAST_SIMPLE]: 'Past Simple',
	[Tense.PAST_CONTINUOUS]: 'Past Continuous',
	[Tense.PAST_PERFECT]: 'Past Perfect',
	[Tense.PAST_PERFECT_CONTINUOUS]: 'Past Perfect Continuous',
	[Tense.FUTURE_SIMPLE]: 'Future Simple',
	[Tense.FUTURE_CONTINUOUS]: 'Future Continuous',
	[Tense.FUTURE_PERFECT]: 'Future Perfect',
	[Tense.FUTURE_PERFECT_CONTINUOUS]: 'Future Perfect Continuous',
};

export type TenseGroupKey = 'present' | 'past' | 'future';

export type ITenseGroup = {
	labelKey: TenseGroupKey;
	tenses: TenseType[];
};

export const TENSE_GROUPS: ITenseGroup[] = [
	{ labelKey: 'present', tenses: TENSES_GROUPS.present },
	{ labelKey: 'past', tenses: TENSES_GROUPS.past },
	{ labelKey: 'future', tenses: TENSES_GROUPS.future },
];
```

- [ ] **Step 2: Remove `MODE_LABELS` from `shared/config/training.ts`**

```ts
export type Step = 'select' | 'training';
export type TrainingMode = 'fixed' | 'infinite';
export type FixedLimit = (typeof FIXED_LIMITS)[number];

export const FIXED_LIMITS = [5, 10, 20] as const;
```

- [ ] **Step 3: Update `TenseGroup.tsx`** — add `'use client'`, `useTranslations`, use `labelKey`

```tsx
'use client';

import { type Tense } from '@/domain/value-objects';
import { Checkbox } from '@/presentation/components/ui/checkbox';
import {
	type ITenseGroup,
	TENSE_LABELS,
} from '@/presentation/web/pages/TenseTrainer/logic/tenseLabels';
import { useTranslations } from 'next-intl';

interface TenseGroupProps {
	group: ITenseGroup;
	selectedTenses: Tense[];
	onToggle: (tense: Tense) => void;
	onToggleGroup: (tenses: Tense[]) => void;
}

const TenseGroup = ({ onToggle, onToggleGroup, group, selectedTenses }: TenseGroupProps) => {
	const t = useTranslations('trainer');
	const allSelected = group.tenses.every(tense => selectedTenses.includes(tense));

	return (
		<div className='flex flex-col gap-2'>
			<div className='flex items-center justify-between'>
				<span className='text-foreground text-sm font-semibold'>
					{t(`group.${group.labelKey}`)}
				</span>
				<button
					onClick={() => onToggleGroup(group.tenses)}
					className='text-primary text-xs hover:underline'
				>
					{allSelected ? t('groupDeselect') : t('groupSelectAll')}
				</button>
			</div>
			<div className='grid grid-cols-1 gap-2 sm:grid-cols-2'>
				{group.tenses.map(tense => (
					<TenseItem
						key={tense}
						tense={tense}
						isSelected={selectedTenses.includes(tense)}
						onToggle={onToggle}
					/>
				))}
			</div>
		</div>
	);
};

export default TenseGroup;

interface TenseItemProps {
	tense: Tense;
	isSelected: boolean;
	onToggle: (tense: Tense) => void;
}

const TenseItem = ({ tense, isSelected, onToggle }: TenseItemProps) => {
	return (
		<label className='border-border bg-card hover:bg-muted flex cursor-pointer items-center gap-3 rounded-lg border p-4 transition-colors'>
			<Checkbox checked={isSelected} onCheckedChange={() => onToggle(tense)} />
			<span className='text-foreground text-sm font-medium'>{TENSE_LABELS[tense]}</span>
		</label>
	);
};
```

- [ ] **Step 4: Update `ModeSelector.tsx`** — replace `MODE_LABELS[m]` with `useTranslations`

```tsx
'use client';

import { FIXED_LIMITS, type FixedLimit, type TrainingMode } from '@/shared/config/training';
import { useTranslations } from 'next-intl';

type Props = {
	mode: TrainingMode;
	fixedLimit: FixedLimit;
	onUpdate: (patch: { mode?: TrainingMode; limit?: FixedLimit }) => void;
};

const MODE_KEY: Record<TrainingMode, 'modeFixed' | 'modeInfinite'> = {
	fixed: 'modeFixed',
	infinite: 'modeInfinite',
};

const ModeSelector = ({ mode, fixedLimit, onUpdate }: Props) => {
	const t = useTranslations('trainer');

	return (
		<div className='border-border bg-card flex flex-col gap-3 rounded-xl border p-5'>
			<p className='text-foreground text-sm font-semibold'>{t('mode')}</p>
			<div className='flex gap-2'>
				{(['fixed', 'infinite'] as TrainingMode[]).map(m => (
					<button
						key={m}
						onClick={() => onUpdate({ mode: m })}
						className={`rounded-lg border px-4 py-2 text-sm transition-colors ${
							mode === m
								? 'border-primary bg-primary text-primary-foreground'
								: 'border-border bg-background text-muted-foreground hover:bg-muted'
						}`}
					>
						{t(MODE_KEY[m])}
					</button>
				))}
			</div>
			<div
				className={`grid transition-all duration-200 ease-in-out ${
					mode === 'fixed' ? 'grid-rows-[1fr]' : '-mt-3 grid-rows-[0fr]'
				}`}
			>
				<div className='overflow-hidden'>
					<div className='flex gap-2'>
						{FIXED_LIMITS.map(limit => (
							<button
								key={limit}
								onClick={() => onUpdate({ limit })}
								className={`rounded-lg border px-4 py-2 text-sm transition-colors ${
									fixedLimit === limit
										? 'border-primary bg-primary text-primary-foreground'
										: 'border-border bg-background text-muted-foreground hover:bg-muted'
								}`}
							>
								{limit}
							</button>
						))}
					</div>
				</div>
			</div>
		</div>
	);
};

export default ModeSelector;
```

- [ ] **Step 5: Update `SelectTrainingSection.tsx`** — replace hardcoded strings

```tsx
'use client';

import { useSessionStore } from '@/client/stores/sessionStore';
import { useSettingsStore } from '@/client/stores/settingsStore';
import { Button } from '@/presentation/components/ui/button';
import { ArrowRightIcon } from 'lucide-react';
import { useTranslations } from 'next-intl';
import { useShallow } from 'zustand/react/shallow';
import { TENSE_GROUPS } from '../../logic/tenseLabels';
import ModeSelector from './ModeSelector';
import TenseGroup from './TenseGroup';

const SelectTrainingSection = () => {
	const t = useTranslations('trainer');
	const tCommon = useTranslations('common');

	const {
		selectedTenses,
		mode,
		fixedLimit,
		toggleTense,
		selectAll,
		clearAll,
		toggleGroup,
		updateMode,
	} = useSettingsStore(
		useShallow(s => ({
			selectedTenses: s.selectedTenses,
			mode: s.mode,
			fixedLimit: s.fixedLimit,
			toggleTense: s.toggleTense,
			selectAll: s.selectAll,
			clearAll: s.clearAll,
			toggleGroup: s.toggleGroup,
			updateMode: s.updateMode,
		})),
	);

	const { exercises, isLoading, setStep, startTraining } = useSessionStore(
		useShallow(s => ({
			exercises: s.exercises,
			isLoading: s.isLoading,
			setStep: s.setStep,
			startTraining: s.startTraining,
		})),
	);

	const handleStartTraining = () => startTraining(selectedTenses, mode, fixedLimit);
	const hasExercises = exercises.length > 0;

	return (
		<main className='bg-background text-foreground flex flex-1 flex-col overflow-hidden'>
			<div className='animate-in fade-in slide-in-from-bottom-4 mx-auto flex w-full max-w-2xl flex-col gap-8 px-6 py-16 duration-300'>
				<div className='flex items-center justify-between'>
					<div>
						<h1 className='text-foreground text-3xl font-bold'>{t('title')}</h1>
						<p className='text-muted-foreground mt-2'>{t('subtitle')}</p>
					</div>
					{hasExercises && (
						<Button variant='ghost' size='sm' onClick={() => setStep('training')}>
							{t('continue')}
							<ArrowRightIcon />
						</Button>
					)}
				</div>

				<ModeSelector mode={mode} fixedLimit={fixedLimit} onUpdate={updateMode} />

				<div className='flex gap-3'>
					<Button variant='outline' size='sm' onClick={selectAll}>
						{t('selectAll')}
					</Button>
					<Button variant='outline' size='sm' onClick={clearAll}>
						{t('clearAll')}
					</Button>
				</div>

				<div className='flex flex-col gap-6'>
					{TENSE_GROUPS.map(group => (
						<TenseGroup
							key={group.labelKey}
							group={group}
							selectedTenses={selectedTenses}
							onToggle={toggleTense}
							onToggleGroup={toggleGroup}
						/>
					))}
				</div>

				<Button
					size='lg'
					onClick={handleStartTraining}
					disabled={selectedTenses.length === 0 || isLoading}
				>
					{isLoading ? tCommon('loading') : hasExercises ? t('restart') : t('start')}
				</Button>
			</div>
		</main>
	);
};

export default SelectTrainingSection;
```

- [ ] **Step 6: Update `TrainingSection.tsx`** — replace hardcoded strings

```tsx
'use client';

import { useSessionStore } from '@/client/stores/sessionStore';
import { useSettingsStore } from '@/client/stores/settingsStore';
import { Badge } from '@/presentation/components/ui/badge';
import { Button } from '@/presentation/components/ui/button';
import { Textarea } from '@/presentation/components/ui/textarea';
import { TENSE_LABELS } from '@/presentation/web/pages/TenseTrainer/logic/tenseLabels';
import { ArrowLeftIcon, ArrowRightIcon } from 'lucide-react';
import { useTranslations } from 'next-intl';
import { useState } from 'react';
import { useShallow } from 'zustand/react/shallow';
import TaskResult from './TaskResult';

const TrainingSection = () => {
	const t = useTranslations('trainer');
	const tCommon = useTranslations('common');

	const {
		exercises,
		currentExerciseIndex,
		isLoading,
		currentAnswer,
		setStep,
		nextExercise,
		submitAnswer,
		finishSession,
	} = useSessionStore(
		useShallow(s => ({
			exercises: s.exercises,
			currentExerciseIndex: s.currentExerciseIndex,
			isLoading: s.isLoading,
			currentAnswer: s.currentAnswer,
			setStep: s.setStep,
			nextExercise: s.nextExercise,
			submitAnswer: s.submitAnswer,
			finishSession: s.finishSession,
		})),
	);

	const { mode, selectedTenses } = useSettingsStore(
		useShallow(s => ({ mode: s.mode, selectedTenses: s.selectedTenses })),
	);

	const current = exercises[currentExerciseIndex];
	const totalExercises = exercises.length;
	const answerRecord = currentAnswer?.exerciseId === current.id ? currentAnswer : null;

	const [userAnswer, setUserAnswer] = useState(answerRecord?.userAnswer ?? '');

	const indexString =
		mode === 'infinite'
			? `# ${currentExerciseIndex + 1}`
			: `${currentExerciseIndex + 1} / ${totalExercises}`;

	const isEmptyAnswer = userAnswer.trim().length === 0;

	return (
		<main className='bg-background text-foreground flex flex-1 flex-col'>
			<div className='mx-auto flex w-full max-w-2xl flex-col gap-8 px-6 py-16'>
				<div className='flex items-center justify-between'>
					<Button variant='ghost' size='sm' className='-ml-2' onClick={() => setStep('select')}>
						<ArrowLeftIcon />
						{t('back')}
					</Button>
					{mode === 'infinite' && (
						<Button
							variant='ghost'
							size='sm'
							className='-mr-2'
							onClick={() => void finishSession()}
						>
							{t('finish')}
							<ArrowRightIcon />
						</Button>
					)}
				</div>

				<div
					key={currentExerciseIndex}
					className='animate-in fade-in slide-in-from-bottom-4 flex flex-col gap-8 duration-300'
				>
					<div className='flex items-center justify-between'>
						{!!answerRecord ? (
							<Badge variant='outline'>{TENSE_LABELS[current.tense]}</Badge>
						) : (
							<div />
						)}
						<span className='text-muted-foreground text-sm'>{indexString}</span>
					</div>

					<div className='border-border bg-card rounded-xl border p-6'>
						<p className='text-foreground text-lg font-medium'>{current.question}</p>
					</div>

					<div className='flex flex-col gap-3'>
						<Textarea
							placeholder={t('inputPlaceholder')}
							value={userAnswer}
							onChange={e => setUserAnswer(e.target.value)}
							disabled={!!answerRecord}
							rows={3}
						/>
						{!answerRecord && (
							<Button
								onClick={() => void submitAnswer(userAnswer, current.id)}
								variant={isEmptyAnswer ? 'outline' : 'default'}
							>
								{isEmptyAnswer ? t('skip') : t('check')}
							</Button>
						)}
					</div>

					{!!answerRecord && (
						<TaskResult
							current={current}
							answerRecord={answerRecord}
							isLoading={isLoading}
							mode={mode}
							currentIndex={currentExerciseIndex}
							totalExercises={totalExercises}
							onNext={() => {
								setUserAnswer('');
								nextExercise(selectedTenses, mode);
							}}
						/>
					)}
				</div>
			</div>
		</main>
	);
};

export default TrainingSection;
```

- [ ] **Step 7: Update `TaskResult.tsx`** — replace hardcoded strings

```tsx
import { type ExerciseAnswer } from '@/domain/entities/Answer';
import { Button } from '@/presentation/components/ui/button';
import { type TrainingMode } from '@/shared/config/training';
import { type ExerciseResponseDto } from '@/shared/dtos';
import { cn } from '@/shared/lib/utils';
import { CheckIcon, XIcon } from 'lucide-react';
import { useTranslations } from 'next-intl';

interface TaskResultProps {
	current: ExerciseResponseDto;
	answerRecord: ExerciseAnswer;
	isLoading: boolean;
	mode: TrainingMode;
	currentIndex: number;
	totalExercises: number;
	onNext: () => void;
}

const TaskResult = ({
	current,
	answerRecord,
	isLoading,
	mode,
	currentIndex,
	totalExercises,
	onNext,
}: TaskResultProps) => {
	const t = useTranslations('trainer');
	const tCommon = useTranslations('common');

	const buttonText = isLoading
		? tCommon('loading')
		: mode === 'infinite' || currentIndex + 1 < totalExercises
			? t('next')
			: t('finish');

	const showResult = !answerRecord.skipped;
	const showCorrectAnswer = answerRecord.skipped || !answerRecord.isCorrect;

	return (
		<div className='animate-in fade-in slide-in-from-bottom-2 flex flex-col gap-4 duration-200'>
			{showResult && (
				<div
					className={cn(
						'flex items-center gap-3 rounded-xl border p-4',
						answerRecord.isCorrect
							? 'border-green-500/30 bg-green-500/10 text-green-600 dark:text-green-400'
							: 'border-red-500/30 bg-red-500/10 text-red-600 dark:text-red-400',
					)}
				>
					{answerRecord.isCorrect ? (
						<CheckIcon className='size-5 shrink-0' />
					) : (
						<XIcon className='size-5 shrink-0' />
					)}
					<span className='text-sm font-medium'>
						{answerRecord.isCorrect ? t('correct') : t('incorrect')}
					</span>
				</div>
			)}
			{showCorrectAnswer && (
				<>
					<div className='border-border bg-card rounded-xl border p-6'>
						<p className='text-muted-foreground mb-1 text-xs font-semibold tracking-wide uppercase'>
							{t('correctAnswer')}
						</p>
						<p className='text-foreground'>{current.answer}</p>
					</div>
					<div className='border-border bg-card rounded-xl border p-6'>
						<p className='text-muted-foreground mb-1 text-xs font-semibold tracking-wide uppercase'>
							{t('whyThisTense')}
						</p>
						<p className='text-foreground text-sm leading-relaxed'>{current.explanation}</p>
					</div>
				</>
			)}
			<Button onClick={onNext} disabled={isLoading}>
				{buttonText}
			</Button>
		</div>
	);
};

export default TaskResult;
```

- [ ] **Step 8: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors.

- [ ] **Step 9: Commit**

```bash
git add presentation/web/pages/TenseTrainer/ shared/config/training.ts
git commit -m "feat: translate Trainer components"
```

---

## Task 9: Translate Profile components

**Files:**

- Modify: `presentation/web/pages/Profile/Profile.tsx`
- Modify: `presentation/web/pages/Profile/ui/StatsOverview/StatsOverview.tsx`
- Modify: `presentation/web/pages/Profile/ui/TenseBreakdown/TenseBreakdown.tsx`
- Modify: `presentation/web/pages/Profile/ui/SessionHistory/SessionHistory.tsx`
- Modify: `presentation/web/pages/Profile/ui/SessionHistory/SessionDetail.tsx`

- [ ] **Step 1: Update `Profile.tsx`**

```tsx
'use client';

import { useTranslations } from 'next-intl';
import { useProfileData } from './logic/useProfileData';
import { ProgressChart } from './ui/ProgressChart/ProgressChart';
import { SessionHistory } from './ui/SessionHistory/SessionHistory';
import { StatsOverview } from './ui/StatsOverview/StatsOverview';
import { TenseBreakdown } from './ui/TenseBreakdown/TenseBreakdown';

const ProfilePage = () => {
	const t = useTranslations('profile');
	const tCommon = useTranslations('common');
	const { overallStats, tenseStats, sessionSummaries, chartData, getSessionAnswers, isLoading } =
		useProfileData();

	if (isLoading) {
		return (
			<main className='flex flex-1 items-center justify-center'>
				<p className='text-muted-foreground text-sm'>{tCommon('loading')}</p>
			</main>
		);
	}

	return (
		<main className='bg-background text-foreground flex flex-1 flex-col'>
			<div className='mx-auto flex w-full max-w-2xl flex-col gap-10 px-6 py-16'>
				<section>
					<h2 className='text-foreground mb-4 text-lg font-semibold'>{t('overallStats')}</h2>
					<StatsOverview stats={overallStats} />
				</section>
				<section>
					<h2 className='text-foreground mb-4 text-lg font-semibold'>{t('progress')}</h2>
					<ProgressChart data={chartData} />
				</section>
				<section>
					<h2 className='text-foreground mb-4 text-lg font-semibold'>{t('byTenses')}</h2>
					<TenseBreakdown stats={tenseStats} />
				</section>
				<section>
					<h2 className='text-foreground mb-4 text-lg font-semibold'>{t('sessionHistory')}</h2>
					<SessionHistory summaries={sessionSummaries} getSessionAnswers={getSessionAnswers} />
				</section>
			</div>
		</main>
	);
};

export default ProfilePage;
```

- [ ] **Step 2: Update `StatsOverview.tsx`** — add `'use client'` + `useTranslations`

```tsx
'use client';

import type { OverallStats } from '@/client/application/services/ProfileService';
import { useTranslations } from 'next-intl';

interface Props {
	stats: OverallStats;
}

export function StatsOverview({ stats }: Props) {
	const t = useTranslations('profile');

	return (
		<div className='grid grid-cols-2 gap-4 sm:grid-cols-4'>
			<StatCard label={t('total')} value={String(stats.total)} />
			<StatCard label={t('correct')} value={String(stats.correct)} sub={`${stats.accuracy}%`} />
			<StatCard label={t('skipped')} value={String(stats.skipped)} />
			<StatCard
				label={t('incorrect')}
				value={String(stats.total - stats.correct - stats.skipped)}
			/>
		</div>
	);
}

function StatCard({ label, value, sub }: { label: string; value: string; sub?: string }) {
	return (
		<div className='border-border bg-card rounded-xl border p-4'>
			<p className='text-muted-foreground text-xs font-semibold tracking-wide uppercase'>{label}</p>
			<p className='text-foreground text-2xl font-bold'>{value}</p>
			{sub && <p className='text-muted-foreground text-sm'>{sub}</p>}
		</div>
	);
}
```

- [ ] **Step 3: Update `TenseBreakdown.tsx`** — add `'use client'` + translate "Нет данных"

```tsx
'use client';

import type { TenseStat } from '@/client/application/services/ProfileService';
import { TENSE_LABELS } from '@/presentation/web/pages/TenseTrainer/logic/tenseLabels';
import { useTranslations } from 'next-intl';

interface Props {
	stats: TenseStat[];
}

export function TenseBreakdown({ stats }: Props) {
	const t = useTranslations('profile');

	if (stats.length === 0) {
		return <p className='text-muted-foreground text-sm'>{t('noData')}</p>;
	}

	return (
		<div className='flex flex-col gap-3'>
			{stats.map(s => (
				<div key={s.tense} className='flex items-center gap-4'>
					<span className='text-foreground w-48 shrink-0 text-sm'>{TENSE_LABELS[s.tense]}</span>
					<div className='bg-muted relative h-2 flex-1 overflow-hidden rounded-full'>
						<div
							className='bg-primary absolute inset-y-0 left-0 rounded-full'
							style={{ width: `${s.accuracy}%` }}
						/>
					</div>
					<span className='text-muted-foreground w-10 text-right text-sm'>{s.accuracy}%</span>
					<span className='text-muted-foreground text-sm'>({s.total})</span>
				</div>
			))}
		</div>
	);
}
```

- [ ] **Step 5: Update `SessionHistory.tsx`**

```tsx
'use client';

import type {
	AnswerWithExercise,
	SessionSummary,
} from '@/client/application/services/ProfileService';
import { Badge } from '@/presentation/components/ui/badge';
import { TENSES_GROUPS } from '@/shared/config/tenses';
import { useLocale, useTranslations } from 'next-intl';
import { useState } from 'react';
import { TENSE_LABELS } from '../../../TenseTrainer/logic/tenseLabels';
import { SessionDetail } from './SessionDetail';

interface Props {
	summaries: SessionSummary[];
	getSessionAnswers: (sessionId: string) => AnswerWithExercise[];
}

export function SessionHistory({ summaries, getSessionAnswers }: Props) {
	const t = useTranslations('profile');
	const locale = useLocale();
	const [openId, setOpenId] = useState<string | null>(null);

	if (summaries.length === 0) {
		return <p className='text-muted-foreground text-sm'>{t('emptyHistory')}</p>;
	}

	return (
		<div className='flex flex-col gap-3'>
			{summaries.map(({ session, total, correct, skipped, accuracy }) => {
				const isOpen = openId === session.id;
				const date = new Date(session.createdAt).toLocaleDateString(locale, {
					day: 'numeric',
					month: 'short',
					hour: '2-digit',
					minute: '2-digit',
				});

				const groupDefs = [
					{ labelKey: 'presentTenses' as const, tenses: TENSES_GROUPS.present },
					{ labelKey: 'pastTenses' as const, tenses: TENSES_GROUPS.past },
					{ labelKey: 'futureTenses' as const, tenses: TENSES_GROUPS.future },
				];

				const groupedTenses = new Set<string>();
				const groupLabels: string[] = [];
				const allTenses = session.tenses.length === 12;

				if (!allTenses) {
					groupDefs.forEach(({ labelKey, tenses }) => {
						if (tenses.every(t2 => session.tenses.includes(t2))) {
							groupLabels.push(t(labelKey));
							tenses.forEach(t2 => groupedTenses.add(t2));
						}
					});
				}

				const finalTenses = allTenses
					? [t('allTenses')]
					: [
							...groupLabels,
							...session.tenses.filter(t2 => !groupedTenses.has(t2)).map(t2 => TENSE_LABELS[t2]),
						];

				return (
					<div key={session.id} className='border-border bg-card rounded-xl border'>
						<button
							className='flex w-full items-start justify-between gap-4 p-4 text-left'
							onClick={() => setOpenId(isOpen ? null : session.id)}
						>
							<div className='flex flex-col gap-1'>
								<span className='text-foreground text-sm font-medium'>{date}</span>
								<div className='flex flex-wrap gap-1'>
									{finalTenses.map(label => (
										<Badge key={label} variant='outline' className='text-xs'>
											{label}
										</Badge>
									))}
								</div>
							</div>
							<div className='text-muted-foreground shrink-0 text-right text-sm'>
								<p>{t('sessionCorrect', { correct, attempted: total - skipped })}</p>
								<p>{accuracy}%</p>
							</div>
						</button>

						{isOpen && (
							<div className='border-border border-t px-4 pb-4'>
								<SessionDetail answers={getSessionAnswers(session.id)} />
							</div>
						)}
					</div>
				);
			})}
		</div>
	);
}
```

- [ ] **Step 6: Update `SessionDetail.tsx`**

```tsx
'use client';

import type { AnswerWithExercise } from '@/client/application/services/ProfileService';
import { TENSE_LABELS } from '@/presentation/web/pages/TenseTrainer/logic/tenseLabels';
import { useTranslations } from 'next-intl';
import { useState } from 'react';

const INITIAL_LIMIT = 5;

interface Props {
	answers: AnswerWithExercise[];
}

export function SessionDetail({ answers }: Props) {
	const t = useTranslations('profile');
	const [showAll, setShowAll] = useState(false);

	if (answers.length === 0) {
		return <p className='text-muted-foreground py-2 text-sm'>{t('noAnswers')}</p>;
	}

	const visible = showAll ? answers : answers.slice(0, INITIAL_LIMIT);
	const remaining = answers.length - INITIAL_LIMIT;

	return (
		<div className='flex flex-col gap-2 pt-2'>
			{visible.map(a => (
				<div key={a.id} className='border-border bg-background rounded-lg border p-3'>
					<div className='mb-1 flex items-center justify-between'>
						<span className='text-muted-foreground text-xs'>{TENSE_LABELS[a.exercise.tense]}</span>
						{a.skipped ? (
							<span className='text-muted-foreground text-xs'>{t('skippedAnswer')}</span>
						) : (
							<span
								className={`text-xs font-medium ${a.isCorrect ? 'text-green-600' : 'text-red-500'}`}
							>
								{a.isCorrect ? t('correctAnswer') : t('incorrectAnswer')}
							</span>
						)}
					</div>
					<p className='text-foreground text-sm'>{a.exercise.question}</p>
					{!a.skipped && (
						<p className='text-muted-foreground mt-1 text-xs'>
							{t('answer', { answer: a.userAnswer })}
						</p>
					)}
				</div>
			))}
			{!showAll && remaining > 0 && (
				<button
					className='text-muted-foreground hover:text-foreground pt-1 text-sm transition-colors'
					onClick={() => setShowAll(true)}
				>
					{t('showMore', { count: remaining })}
				</button>
			)}
		</div>
	);
}
```

- [ ] **Step 7: Verify TypeScript compiles**

```bash
npm run typecheck
```

Expected: no errors.

- [ ] **Step 8: Commit**

```bash
git add presentation/web/pages/Profile/
git commit -m "feat: translate Profile components"
```

---

## Task 10: Translate common components and add language selector to Header

**Files:**

- Modify: `presentation/web/components/NetworkBadge/NetworkBadge.tsx`
- Modify: `presentation/web/components/InstallBanner/InstallBanner.tsx`
- Modify: `presentation/web/components/Header/Header.tsx`

- [ ] **Step 1: Update `NetworkBadge.tsx`**

```tsx
'use client';

import { Badge } from '@/presentation/components/ui/badge';
import { cn } from '@/shared/lib/utils';
import { useNetworkStatus } from '@/shared/hooks/useNetworkStatus';
import { useTranslations } from 'next-intl';

const NetworkBadge = () => {
	const t = useTranslations('network');
	const { status } = useNetworkStatus();

	if (status === 'online') return null;

	const label = status === 'offline' ? t('offline') : t('slow');
	const isOffline = status === 'offline';

	return (
		<>
			<span
				className={cn(
					'size-2 rounded-full md:hidden',
					isOffline ? 'bg-destructive' : 'bg-yellow-500',
				)}
			/>
			<Badge className='hidden md:inline-flex' variant={isOffline ? 'destructive' : 'outline'}>
				{label}
			</Badge>
		</>
	);
};

export default NetworkBadge;
```

- [ ] **Step 2: Update `InstallBanner.tsx`**

```tsx
'use client';

import { Button } from '@/presentation/components/ui/button';
import { useInstallPrompt } from '@/shared/hooks/useInstallPrompt';
import { useTranslations } from 'next-intl';
import { X } from 'lucide-react';

const InstallBanner = () => {
	const t = useTranslations('install');
	const { canInstall, install, dismiss } = useInstallPrompt();

	if (!canInstall) return null;

	return (
		<div className='bg-background border-border fixed right-4 bottom-4 left-4 z-50 flex items-center justify-between gap-4 rounded-xl border p-4 shadow-lg md:right-6 md:left-auto md:w-80'>
			<p className='text-sm'>
				<span className='text-foreground font-medium'>{t('title')}</span>
				<br />
				<span className='text-muted-foreground'>{t('description')}</span>
			</p>
			<div className='flex shrink-0 items-center gap-2'>
				<Button size='sm' onClick={install}>
					{t('installButton')}
				</Button>
				<Button variant='ghost' size='icon-sm' onClick={dismiss}>
					<X />
				</Button>
			</div>
		</div>
	);
};

export default InstallBanner;
```

- [ ] **Step 3: Update `Header.tsx`** — add nav translations + language selector

```tsx
'use client';

import { Button } from '@/presentation/components/ui/button';
import { Link, usePathname, useRouter } from '@/shared/i18n/navigation';
import { routing, type Locale } from '@/shared/i18n/config';
import { cn } from '@/shared/lib/utils';
import { useSwipeNavigation } from '@/shared/hooks/useSwipeNavigation';
import { NAV_ROUTES, ROUTES } from '@/shared/config/routes';
import { User } from 'lucide-react';
import { useLocale, useTranslations } from 'next-intl';
import NetworkBadge from '../NetworkBadge/NetworkBadge';

const LOCALE_LABELS: Record<Locale, string> = {
	ru: 'RU',
	fr: 'FR',
	de: 'DE',
	es: 'ES',
};

const Header = () => {
	const t = useTranslations('nav');
	const locale = useLocale() as Locale;
	const pathname = usePathname();
	const router = useRouter();
	useSwipeNavigation([...NAV_ROUTES]);

	const NAV_LINKS = [
		{ href: ROUTES.home, label: t('home') },
		{ href: ROUTES.trainer, label: t('trainer') },
	];

	const handleLocaleChange = (e: React.ChangeEvent<HTMLSelectElement>) => {
		router.replace(pathname, { locale: e.target.value as Locale });
	};

	return (
		<header className='border-border bg-background/90 sticky top-0 z-50 border-b backdrop-blur-md'>
			<div className='mx-auto flex h-16 max-w-5xl items-center justify-between px-6'>
				<div className='flex items-center gap-10'>
					<Link href={ROUTES.home} aria-label='Tense Master' className='flex items-center gap-2'>
						<span
							aria-hidden='true'
							className='bg-primary text-primary-foreground flex size-7 items-center justify-center rounded-md text-xs font-bold'
						>
							T
						</span>
						<span
							aria-hidden='true'
							className='text-foreground hidden text-sm font-semibold tracking-tight md:block'
						>
							Tense<span className='text-primary'>Master</span>
						</span>
					</Link>

					<nav aria-label={t('mainNavLabel')} className='flex items-center gap-0.5'>
						{NAV_LINKS.map(link => (
							<Link
								key={link.href}
								href={link.href}
								className={cn(
									'relative px-4 py-1.5 text-sm font-medium transition-colors',
									pathname === link.href
										? 'text-foreground'
										: 'text-muted-foreground hover:text-foreground',
								)}
							>
								{link.label}
								{pathname === link.href && (
									<span className='bg-primary absolute inset-x-2 -bottom-[17px] h-0.5 rounded-full' />
								)}
							</Link>
						))}
					</nav>
				</div>

				<div className='flex items-center gap-2'>
					<NetworkBadge />
					<select
						value={locale}
						onChange={handleLocaleChange}
						aria-label={t('languageSelectorLabel')}
						className='text-foreground bg-background border-border rounded-md border px-2 py-1 text-xs font-medium'
					>
						{routing.locales.map(l => (
							<option key={l} value={l}>
								{LOCALE_LABELS[l]}
							</option>
						))}
					</select>
					<Button
						variant='ghost'
						size='icon-sm'
						className={cn(
							pathname === ROUTES.profile && 'border-border bg-card text-card-foreground border',
						)}
						asChild
					>
						<Link href={ROUTES.profile} aria-label={t('profileAriaLabel')}>
							<User />
						</Link>
					</Button>
				</div>
			</div>
		</header>
	);
};

export default Header;
```

- [ ] **Step 4: Verify TypeScript and lint**

```bash
npm run typecheck && npm run lint:check
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add presentation/web/components/
git commit -m "feat: translate common components and add language selector to Header"
```

---

## Task 11: Add fr/de/es translations

**Files:**

- Create: `shared/i18n/messages/fr.json`
- Create: `shared/i18n/messages/de.json`
- Create: `shared/i18n/messages/es.json`

- [ ] **Step 1: Create `fr.json`**

```json
{
	"common": {
		"loading": "Chargement..."
	},
	"nav": {
		"home": "Accueil",
		"trainer": "Entraîneur",
		"mainNavLabel": "Navigation principale",
		"profileAriaLabel": "Profil utilisateur",
		"languageSelectorLabel": "Sélection de la langue"
	},
	"home": {
		"tagline": "Entraîne-toi aux temps anglais hors ligne",
		"heroDescription": "La confusion avec les <bold>temps anglais</bold> est l'un des problèmes les plus fréquents dans l'apprentissage de la <bold>langue</bold>. Non pas parce que les règles sont complexes, mais parce qu'il faut les ressentir en contexte. Cet <bold>entraîneur</bold> est fait pour ça.",
		"startTraining": "Commencer l'entraînement",
		"startTrainingAriaLabel": "Commencer l'entraînement aux temps anglais",
		"whyHeading": "Pourquoi cet entraîneur",
		"privacy": {
			"title": "Vos données restent chez vous",
			"localStorage": {
				"title": "Stockage local",
				"description": "Toute la progression est stockée dans votre navigateur — elle ne va nulle part."
			},
			"noTracking": {
				"title": "Sans surveillance",
				"description": "Nous ne collectons, ne stockons ni ne transmettons aucune donnée vous concernant."
			},
			"offline": {
				"title": "Fonctionne hors ligne",
				"description": "Après le premier chargement, l'application fonctionne sans internet."
			}
		},
		"reasons": {
			"context": {
				"title": "Le contexte décide tout",
				"description": "En anglais, la même action peut être exprimée par différents <bold>temps</bold> selon le sens que vous souhaitez exprimer."
			},
			"forms": {
				"title": "12 temps — ce n'est pas un mythe",
				"description": "Chaque <bold>temps</bold> a ses marqueurs et sa logique. Sans pratique, ils se mélangent et cessent d'être intuitifs."
			},
			"noTools": {
				"title": "Pas d'entraîneurs efficaces",
				"description": "Je n'ai trouvé aucun outil qui <bold>entraîne</bold> vraiment le choix du temps selon le contexte — alors je l'ai créé."
			}
		}
	},
	"trainer": {
		"title": "Tense Trainer",
		"subtitle": "Choisissez les temps à pratiquer",
		"continue": "Continuer",
		"selectAll": "Tout sélectionner",
		"clearAll": "Réinitialiser",
		"start": "Commencer",
		"restart": "Recommencer",
		"back": "Retour",
		"finish": "Terminer",
		"inputPlaceholder": "Entrez la traduction en anglais...",
		"skip": "Passer",
		"check": "Vérifier",
		"correct": "Correct !",
		"incorrect": "Incorrect",
		"correctAnswer": "Bonne réponse",
		"whyThisTense": "Pourquoi ce temps",
		"next": "Suivant",
		"mode": "Mode",
		"modeFixed": "Nombre fixe",
		"modeInfinite": "Mode infini",
		"groupSelectAll": "Tout sélectionner",
		"groupDeselect": "Désélectionner",
		"group": {
			"present": "Present",
			"past": "Past",
			"future": "Future"
		}
	},
	"profile": {
		"overallStats": "Statistiques générales",
		"progress": "Progression",
		"byTenses": "Par temps",
		"sessionHistory": "Historique des sessions",
		"total": "Total",
		"correct": "Correct",
		"skipped": "Passé",
		"incorrect": "Incorrect",
		"emptyHistory": "Historique vide",
		"noData": "Pas de données",
		"noAnswers": "Pas de réponses",
		"showMore": "Afficher encore {count}",
		"sessionCorrect": "{correct}/{attempted} correct",
		"skippedAnswer": "Passé",
		"correctAnswer": "Correct",
		"incorrectAnswer": "Incorrect",
		"answer": "Réponse : {answer}",
		"allTenses": "Tous les temps",
		"presentTenses": "Present tenses",
		"pastTenses": "Past tenses",
		"futureTenses": "Future tenses"
	},
	"network": {
		"offline": "Hors ligne",
		"slow": "Connexion lente"
	},
	"install": {
		"title": "Installer Tense Master",
		"description": "Utiliser hors ligne, accès plus rapide",
		"installButton": "Installer"
	}
}
```

- [ ] **Step 2: Create `de.json`**

```json
{
	"common": {
		"loading": "Laden..."
	},
	"nav": {
		"home": "Startseite",
		"trainer": "Trainer",
		"mainNavLabel": "Hauptnavigation",
		"profileAriaLabel": "Benutzerprofil",
		"languageSelectorLabel": "Sprachauswahl"
	},
	"home": {
		"tagline": "Übe englische Zeitformen offline",
		"heroDescription": "Verwirrung mit <bold>englischen Zeitformen</bold> ist eines der häufigsten Probleme beim Sprachenlernen. Nicht weil die Regeln schwierig sind, sondern weil man sie im Kontext fühlen muss. Dieser <bold>Trainer</bold> ist genau dafür gemacht.",
		"startTraining": "Training starten",
		"startTrainingAriaLabel": "Training für englische Zeitformen starten",
		"whyHeading": "Warum dieser Trainer",
		"privacy": {
			"title": "Ihre Daten bleiben bei Ihnen",
			"localStorage": {
				"title": "Lokaler Speicher",
				"description": "Der gesamte Fortschritt wird in Ihrem Browser gespeichert — er geht nirgendwo hin."
			},
			"noTracking": {
				"title": "Ohne Tracking",
				"description": "Wir sammeln, speichern oder übertragen keine Daten über Sie."
			},
			"offline": {
				"title": "Funktioniert offline",
				"description": "Nach dem ersten Laden funktioniert die App ohne Internet."
			}
		},
		"reasons": {
			"context": {
				"title": "Kontext entscheidet alles",
				"description": "Im Englischen kann dieselbe Handlung je nach Bedeutung durch verschiedene <bold>Zeitformen</bold> ausgedrückt werden."
			},
			"forms": {
				"title": "12 Zeitformen — kein Mythos",
				"description": "Jede <bold>Zeitform</bold> hat ihre eigenen Marker und Logik. Ohne Übung vermischen sie sich und werden nicht mehr intuitiv."
			},
			"noTools": {
				"title": "Keine funktionierenden Trainer",
				"description": "Ich habe kein einziges Tool gefunden, das wirklich die Wahl der Zeitform nach Kontext <bold>trainiert</bold> — also habe ich es selbst erstellt."
			}
		}
	},
	"trainer": {
		"title": "Tense Trainer",
		"subtitle": "Wähle die zu übenden Zeitformen",
		"continue": "Weiter",
		"selectAll": "Alle auswählen",
		"clearAll": "Zurücksetzen",
		"start": "Starten",
		"restart": "Neu starten",
		"back": "Zurück",
		"finish": "Beenden",
		"inputPlaceholder": "Übersetzung auf Englisch eingeben...",
		"skip": "Überspringen",
		"check": "Prüfen",
		"correct": "Richtig!",
		"incorrect": "Falsch",
		"correctAnswer": "Richtige Antwort",
		"whyThisTense": "Warum diese Zeitform",
		"next": "Nächste",
		"mode": "Modus",
		"modeFixed": "Feste Anzahl",
		"modeInfinite": "Unendlicher Modus",
		"groupSelectAll": "Alle auswählen",
		"groupDeselect": "Abwählen",
		"group": {
			"present": "Present",
			"past": "Past",
			"future": "Future"
		}
	},
	"profile": {
		"overallStats": "Gesamtstatistik",
		"progress": "Fortschritt",
		"byTenses": "Nach Zeitformen",
		"sessionHistory": "Sitzungsverlauf",
		"total": "Gesamt",
		"correct": "Richtig",
		"skipped": "Übersprungen",
		"incorrect": "Falsch",
		"emptyHistory": "Verlauf leer",
		"noData": "Keine Daten",
		"noAnswers": "Keine Antworten",
		"showMore": "Noch {count} anzeigen",
		"sessionCorrect": "{correct}/{attempted} richtig",
		"skippedAnswer": "Übersprungen",
		"correctAnswer": "Richtig",
		"incorrectAnswer": "Falsch",
		"answer": "Antwort: {answer}",
		"allTenses": "Alle Zeitformen",
		"presentTenses": "Present tenses",
		"pastTenses": "Past tenses",
		"futureTenses": "Future tenses"
	},
	"network": {
		"offline": "Offline",
		"slow": "Langsame Verbindung"
	},
	"install": {
		"title": "Tense Master installieren",
		"description": "Offline nutzen, schnellerer Zugriff",
		"installButton": "Installieren"
	}
}
```

- [ ] **Step 3: Create `es.json`**

```json
{
	"common": {
		"loading": "Cargando..."
	},
	"nav": {
		"home": "Inicio",
		"trainer": "Entrenador",
		"mainNavLabel": "Navegación principal",
		"profileAriaLabel": "Perfil de usuario",
		"languageSelectorLabel": "Selección de idioma"
	},
	"home": {
		"tagline": "Practica los tiempos verbales ingleses sin conexión",
		"heroDescription": "La confusión con los <bold>tiempos verbales ingleses</bold> es uno de los problemas más frecuentes al aprender el <bold>idioma</bold>. No porque las reglas sean difíciles, sino porque hay que sentirlos en contexto. Este <bold>entrenador</bold> es exactamente para eso.",
		"startTraining": "Comenzar entrenamiento",
		"startTrainingAriaLabel": "Comenzar entrenamiento de tiempos verbales ingleses",
		"whyHeading": "Por qué este entrenador",
		"privacy": {
			"title": "Tus datos se quedan contigo",
			"localStorage": {
				"title": "Almacenamiento local",
				"description": "Todo el progreso se almacena en tu navegador — no va a ningún lugar."
			},
			"noTracking": {
				"title": "Sin rastreo",
				"description": "No recopilamos, almacenamos ni transmitimos ningún dato sobre ti."
			},
			"offline": {
				"title": "Funciona sin conexión",
				"description": "Después de la primera carga, la aplicación funciona sin internet."
			}
		},
		"reasons": {
			"context": {
				"title": "El contexto lo decide todo",
				"description": "En inglés, la misma acción puede expresarse con diferentes <bold>tiempos</bold> según el significado que quieras transmitir."
			},
			"forms": {
				"title": "12 tiempos — no es un mito",
				"description": "Cada <bold>tiempo</bold> tiene sus marcadores y lógica. Sin práctica, se mezclan y dejan de ser intuitivos."
			},
			"noTools": {
				"title": "No hay entrenadores funcionales",
				"description": "No encontré ninguna herramienta que realmente <bold>entrene</bold> la elección del tiempo según el contexto — así que lo creé yo mismo."
			}
		}
	},
	"trainer": {
		"title": "Tense Trainer",
		"subtitle": "Elige los tiempos a practicar",
		"continue": "Continuar",
		"selectAll": "Seleccionar todos",
		"clearAll": "Restablecer",
		"start": "Comenzar",
		"restart": "Comenzar de nuevo",
		"back": "Atrás",
		"finish": "Finalizar",
		"inputPlaceholder": "Ingresa la traducción en inglés...",
		"skip": "Saltar",
		"check": "Verificar",
		"correct": "¡Correcto!",
		"incorrect": "Incorrecto",
		"correctAnswer": "Respuesta correcta",
		"whyThisTense": "Por qué este tiempo",
		"next": "Siguiente",
		"mode": "Modo",
		"modeFixed": "Cantidad fija",
		"modeInfinite": "Modo infinito",
		"groupSelectAll": "Seleccionar todos",
		"groupDeselect": "Deseleccionar",
		"group": {
			"present": "Present",
			"past": "Past",
			"future": "Future"
		}
	},
	"profile": {
		"overallStats": "Estadísticas generales",
		"progress": "Progreso",
		"byTenses": "Por tiempos",
		"sessionHistory": "Historial de sesiones",
		"total": "Total",
		"correct": "Correcto",
		"skipped": "Saltado",
		"incorrect": "Incorrecto",
		"emptyHistory": "Historial vacío",
		"noData": "Sin datos",
		"noAnswers": "Sin respuestas",
		"showMore": "Mostrar {count} más",
		"sessionCorrect": "{correct}/{attempted} correcto",
		"skippedAnswer": "Saltado",
		"correctAnswer": "Correcto",
		"incorrectAnswer": "Incorrecto",
		"answer": "Respuesta: {answer}",
		"allTenses": "Todos los tiempos",
		"presentTenses": "Present tenses",
		"pastTenses": "Past tenses",
		"futureTenses": "Future tenses"
	},
	"network": {
		"offline": "Sin conexión",
		"slow": "Conexión lenta"
	},
	"install": {
		"title": "Instalar Tense Master",
		"description": "Usar sin conexión, acceso más rápido",
		"installButton": "Instalar"
	}
}
```

- [ ] **Step 4: Commit**

```bash
git add shared/i18n/messages/
git commit -m "feat: add fr/de/es translations"
```

---

## Task 12: Final verification

- [ ] **Step 1: Run full CI check**

```bash
npm run ci:check
```

Expected: typecheck, format, lint, and tests all pass.

> If existing component tests fail because `useTranslations` is called without a provider, wrap the rendered component with `NextIntlClientProvider` and pass a minimal messages object. Example:
>
> ```tsx
> import { NextIntlClientProvider } from 'next-intl';
> import messages from '@/shared/i18n/messages/ru.json';
>
> render(
> 	<NextIntlClientProvider locale='ru' messages={messages}>
> 		<ComponentUnderTest />
> 	</NextIntlClientProvider>,
> );
> ```

- [ ] **Step 2: Manual smoke test in dev**

```bash
npm run dev
```

Verify:

1. `http://localhost:3000` → redirects to `/ru/`
2. Language selector in Header switches between `ru`, `fr`, `de`, `es` — URL changes accordingly
3. All page text is translated in the selected language
4. Navigation links work (Home, Trainer, Profile)
5. Active link indicator still works in Header
6. `http://localhost:3000/de/tense-trainer` — trainer page in German
7. PWA manifest: `http://localhost:3000/ru/manifest.webmanifest` → should serve (if manifest.ts was added; otherwise `manifest.json` still works via `start_url: "/"`)

- [ ] **Step 3: Commit**

```bash
git add .
git commit -m "feat: complete i18n Phase 1 — ru/fr/de/es UI translations with URL routing"
```
